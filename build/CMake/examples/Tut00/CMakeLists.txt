set(NAZARA_SOURCE_TUT00_DIRECTORY ${NAZARA_EXAMPLES_DIRECTORY}Tut00)

include_directories(
		${NAZARA_INCLUDE_DIRECTORY}
		${NAZARA_NDK_INCLUDE_DIRECTORY}
	)

file(GLOB FIRSTSCENE_SOURCE
		${NAZARA_SOURCE_TUT00_DIRECTORY}/*.cpp
	)

set(EXEC_NAME ${NAZARA_EXEC_NAME}Tut00)

link_directories(${NAZARA_LIBRARY_DIRECTORY})

add_executable(${EXEC_NAME} ${FIRSTSCENE_SOURCE})

target_link_libraries(${NAZARA_EXEC_NAME}Tut00
		${NAZARA_LIB_AUDIO}
		${NAZARA_LIB_CORE}
		${NAZARA_LIB_GRAPHICS}
		${NAZARA_LIB_LUA}
		${NAZARA_LIB_PHYSICS}
		${NAZARA_LIB_RENDERER}
		${NAZARA_LIB_UTILITY}

		${NAZARA_LIB_SDK}
	)
