set(NAZARA_SOURCE_FIRSTSCENE_DIRECTORY ${NAZARA_EXAMPLES_DIRECTORY}FirstScene)

include_directories(
		${NAZARA_INCLUDE_DIRECTORY}
		${NAZARA_NDK_INCLUDE_DIRECTORY}
	)

file(GLOB FIRSTSCENE_SOURCE
		${NAZARA_SOURCE_FIRSTSCENE_DIRECTORY}/*.cpp
	)

set(EXEC_NAME ${NAZARA_EXEC_NAME}FirstScene)

link_directories(${NAZARA_LIBRARY_DIRECTORY})

add_executable(${EXEC_NAME} ${FIRSTSCENE_SOURCE})

target_link_libraries(${NAZARA_EXEC_NAME}FirstScene
		${NAZARA_LIB_AUDIO}
		${NAZARA_LIB_CORE}
		${NAZARA_LIB_GRAPHICS}
		${NAZARA_LIB_LUA}
		${NAZARA_LIB_PHYSICS}
		${NAZARA_LIB_RENDERER}
		${NAZARA_LIB_UTILITY}

		${NAZARA_LIB_SDK}
	)
