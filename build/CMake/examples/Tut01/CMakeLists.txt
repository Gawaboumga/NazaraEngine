project("Tut01_HelloWorld")

set(NAZARA_SOURCE_TUT01_DIRECTORY ${NAZARA_EXAMPLES_DIRECTORY}Tut01/)

include_directories(
		${NAZARA_INCLUDE_DIRECTORY}
		${SDK_INCLUDE_DIRECTORY}
	)

file(GLOB FIRSTSCENE_SOURCE
		${NAZARA_SOURCE_TUT01_DIRECTORY}*.cpp
	)

set(EXEC_NAME ${NAZARA_DEMO_NAME}Tut01)

link_directories(${NAZARA_LIBRARY_DIRECTORY})

set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${NAZARA_DEMO_OUTPUT_DIRECTORY})

add_executable(${EXEC_NAME} ${FIRSTSCENE_SOURCE})

target_link_libraries(${EXEC_NAME}
		${NAZARA_LIB_AUDIO}
		${NAZARA_LIB_CORE}
		${NAZARA_LIB_GRAPHICS}
		${NAZARA_LIB_LUA}
		${NAZARA_LIB_NETWORK}
		${NAZARA_LIB_NOISE}
		${NAZARA_LIB_PHYSICS}
		${NAZARA_LIB_RENDERER}
		${NAZARA_LIB_UTILITY}

		${NAZARA_LIB_SDK}
	)
