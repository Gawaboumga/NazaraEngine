set(NAZARA_SOURCE_CORE_DIRECTORY ${NAZARA_SOURCE_DIRECTORY}Nazara/Noise/)

include_directories(
		${NAZARA_INCLUDE_DIRECTORY}
		${NAZARA_SOURCE_DIRECTORY}
		${NAZARA_EXTLIBS_DIRECTORY}/include/
	)

file(GLOB NOISE_SOURCE
		${NAZARA_SOURCE_CORE_DIRECTORY}/Debug/*.cpp
		${NAZARA_SOURCE_CORE_DIRECTORY}/*.cpp
	)

link_directories(${NAZARA_EXTLIBS_DIRECTORY}/lib/gmake/x64/)

add_library(${NAZARA_LIBRARY_NAME}Noise${NAZARA_LIBRARY_EXTENSION} ${BUILD_SHARED_LIBS} ${NOISE_SOURCE})

target_link_libraries(${NAZARA_LIBRARY_NAME}Noise${NAZARA_LIBRARY_EXTENSION})
