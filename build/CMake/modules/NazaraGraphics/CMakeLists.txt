project("NazaraGraphics")

set(NAZARA_SOURCE_CORE_DIRECTORY ${NAZARA_SOURCE_DIRECTORY}Nazara/Graphics/)

include_directories(
		${NAZARA_INCLUDE_DIRECTORY}
		${NAZARA_SOURCE_DIRECTORY}
		${NAZARA_EXTLIBS_INCLUDE_DIRECTORY}
	)

file(GLOB UTILITY_SOURCE
		${NAZARA_SOURCE_CORE_DIRECTORY}Debug/*.cpp
		${NAZARA_SOURCE_CORE_DIRECTORY}Formats/*.cpp
		${NAZARA_SOURCE_CORE_DIRECTORY}*.cpp
	)

link_directories(${NAZARA_EXTLIBS_OUTPUT_DIRECTORY})

set(CMAKE_LIBRARY_OUTPUT_DIRECTORY ${NAZARA_LIBS_OUTPUT_DIRECTORY})

add_library(${NAZARA_LIBRARY_NAME}Graphics${NAZARA_LIBRARY_EXTENSION} ${BUILD_SHARED_LIBS} ${UTILITY_SOURCE})

target_link_libraries(${NAZARA_LIBRARY_NAME}Graphics${NAZARA_LIBRARY_EXTENSION})
