set(SDK_SOURCE_NDK_DIRECTORY ${SDK_SOURCE_DIRECTORY}NDK/)

include_directories(
		${NAZARA_INCLUDE_DIRECTORY}
		${SDK_INCLUDE_DIRECTORY}
		${SDK_SOURCE_DIRECTORY}
		${SDK_EXTLIBS_DIRECTORY}/include/
	)

file(GLOB NDK_SOURCE
		${SDK_SOURCE_NDK_DIRECTORY}/Components/*.cpp
		${SDK_SOURCE_NDK_DIRECTORY}/Systems/*.cpp
		${SDK_SOURCE_NDK_DIRECTORY}/*.cpp
	)

link_directories(${SDK_EXTLIBS_DIRECTORY}/lib/gmake/x64/)

add_library(${SDK_LIBRARY_NAME}SDK${SDK_LIBRARY_EXTENSION} ${BUILD_SHARED_LIBS} ${NDK_SOURCE})

target_link_libraries(${SDK_LIBRARY_NAME}SDK${SDK_LIBRARY_EXTENSION}
		${NAZARA_LIB_AUDIO}
		${NAZARA_LIB_CORE}
		${NAZARA_LIB_GRAPHICS}
		${NAZARA_LIB_LUA}
		${NAZARA_LIB_NETWORK}
		${NAZARA_LIB_NOISE}
		${NAZARA_LIB_PHYSICS}
		${NAZARA_LIB_RENDERER}
		${NAZARA_LIB_UTILITY}
	)
